{% extends 'base.html.twig' %}

{% block title %}Modifier un Wish
{% endblock %}

{% block body %}
	<div class="col-4">
		<h1>Modifier Wish</h1>
		{{ form_start(formWish) }}
		{{ form_widget(formWish) }}
		<button type="submit" class="btn btn-primary">
			<i class="fas fa-edit"></i>
		</button>
		{{ form_end(formWish) }}
	</div>
{% endblock %}
