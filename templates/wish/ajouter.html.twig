{% extends 'base.html.twig' %}

{% block title %}Ajouter un Wish
{% endblock %}

{% block body %}
	<div class="col-4">
		<h1>Ajouter Wish</h1>
		{{ form_start(formWish) }}
		{{ form_widget(formWish) }}
		<button type="submit" class="btn btn-success">
			<i class="fas fa-plus"></i>
		</button>
		{{ form_end(formWish) }}
	</div>
{% endblock %}
